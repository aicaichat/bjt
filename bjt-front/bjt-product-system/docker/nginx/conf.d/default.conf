server {
    listen       80;
    server_name  localhost;
    root   /var/www/html;
    index  index.php index.html index.htm;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # 添加安全相关的响应头
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    # Specific rule for /wp-json/ to ensure it goes to WordPress
    location /wp-json/ {
        error_log /var/log/nginx/wp_json_error.log debug;
        access_log /var/log/nginx/wp_json_access.log;
        proxy_pass http://wordpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
    }

    # WordPress admin 和 API 请求路由
    location ~ ^/(wp-admin|wp-login\.php) {
        error_log /var/log/nginx/wp_admin_error.log debug;
        access_log /var/log/nginx/wp_admin_access.log;
        proxy_pass http://wordpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # PHP 文件处理
    location ~ \.php$ {
        proxy_pass http://wordpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 添加对 debug 目录的支持
    location ~ ^/debug/ {
        proxy_pass http://wordpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 添加对 wp-content 目录的支持
    location ~ ^/wp-content/ {
        try_files $uri $uri/ =404;
    }

    # 添加对 uploads 目录的支持（上传文件访问）
    location /uploads/ {
        alias /usr/share/nginx/html/uploads/;
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
        access_log off;
        
        # 允许跨域访问（如果需要）
        add_header Access-Control-Allow-Origin *;
        
        # 处理文件不存在的情况，支持带空格的文件名
        try_files $uri $uri/ =404;
        
        # 禁用自动索引
        autoindex off;
    }

    # 静态文件缓存设置
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
        try_files $uri $uri/ =404;
    }

    # 默认路由到WordPress（前端容器未启动时）
    location / {
        error_log /var/log/nginx/error.log debug;
        proxy_pass http://wordpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 错误页面配置
    error_page   404              /404.html;
    error_page   500 502 503 504  /50x.html;
    
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # 禁止访问 . 开头的隐藏文件
    location ~ /\. {
        deny  all;
    }
} 