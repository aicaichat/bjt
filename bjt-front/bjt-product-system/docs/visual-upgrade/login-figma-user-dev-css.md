# 前台商城登录 — Figma Dev Mode CSS 原文（信源存档）

**路由**：`/login`  
**实现文件**：`frontend/src/pages/Login/index.tsx`、`frontend/src/pages/Login/Login.css`  
**域 token**：`frontend/src/styles/figma-tokens-login.css`

**状态**：索引占位。使用 Skill「以下 CSS 为唯一信源」做登录页整页对齐时，Agent 应将对话中 `css` 围栏全文写入 `docs/visual-upgrade/sources/login-figma-dev-export-YYYY-MM-DD.css`，并在此 **追加链接与日期**。

**块枚举防漏**：`.cursor/skills/css-figma-strict-implementation/reference-login-full-alignment.md`（L1–L8）。

## 已入库文件

- [`sources/login-figma-dev-export-2026-04-12.css`](sources/login-figma-dev-export-2026-04-12.css) — 前台 `/login` 整页 Dev 原文（与用户对话「以下 CSS 为唯一信源」一致）。

### 资源文件（稿中 url，需自行放入 `frontend/public`）

- 背景：`微信图片_20181207152621-1.jpg` → 实现使用 **`/images/login-bg-figma.jpg`**（请将稿图复制为该路径以便 `background-image` 生效）。
- Logo：`Air-Paper 蓝色 (R标)` → 实现沿用现有 **`/images/logo-1.webp`**，尺寸按稿 **376×43** 约束。
