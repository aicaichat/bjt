# 机器选型页 — Figma Dev Mode CSS 原文（信源存档）

**状态**：占位索引。首次由 Agent 在「以下 CSS 为唯一信源」任务中写入 `sources/machine-selection-figma-dev-export-YYYY-MM-DD.css` 后，在此 **链接** 并注明日期与覆盖范围（如 ProductLine1 单卡 / 筛选条）。

**实现层同步**：`frontend/src/styles/figma-tokens-machine-selection.css`、`frontend/src/figma-tokens/machine-selection.json`、`frontend/src/styles/machine-selection-figma.css`；布局链见 `.cursor/rules/figma-machine-selection-layout.mdc`。

## 维护约定

1. **Skill `css-figma-strict-implementation`**：机器选型整页对齐时，**须**将对话中 `css` 围栏全文写入 `docs/visual-upgrade/sources/machine-selection-figma-dev-export-YYYY-MM-DD.css`，并更新本 md 的链接列表。
2. 块枚举防漏：`.cursor/skills/css-figma-strict-implementation/reference-machine-selection-full-alignment.md`（M1–M12）。

## 已入库文件

| 日期 | 文件 | 范围 | 链接 |
|------|------|------|------|
| 2026-04-12 | `machine-selection-figma-dev-export-2026-04-12.css` | Frame 1013 (侧栏) + Frame 529 (主内容) + Frame 471/469 (机器卡片) + Frame 205 (图库) + Frame 1017 (操作区) + 分页 | [查看](./sources/machine-selection-figma-dev-export-2026-04-12.css) |
| 2026-04-12 | 同上 | 配件页面: Frame 1012 (侧栏 303×594) + Frame 529 (主内容 1617×1137) | 同上 |
