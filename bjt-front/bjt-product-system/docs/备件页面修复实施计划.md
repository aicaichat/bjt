# 备件页面修复实施计划

## 执行前检查清单

### ✅ 准备工作完成情况
- [x] 字段对照检查表已创建
- [x] 字段完整性检查脚本已创建
- [x] 关键问题已识别
- [x] 修复优先级已确定

## 阶段一：代码备份和环境准备 (风险：极低)

### 1.1 备份现有代码
```bash
# 备份当前版本
cp frontend/src/pages/SpareParts/index.tsx frontend/src/pages/SpareParts/index.tsx.backup.$(date +%Y%m%d_%H%M%S)

# 备份相关类型定义
cp frontend/src/types/spareParts.ts frontend/src/types/spareParts.ts.backup.$(date +%Y%m%d_%H%M%S)
```

### 1.2 运行基线检查
```bash
# 在浏览器控制台运行检查脚本
node docs/字段完整性检查脚本.js
```

## 阶段二：数据类型定义修正 (风险：低)

### 2.1 修正 TypeScript 接口 
**文件：`frontend/src/types/spareParts.ts`**

```typescript
// 修正前
interface SparePart {
  // ...
  is_consumable?: boolean | null;
}

// 修正后
interface SparePart {
  // ...
  is_consumable: 1 | 2 | 3; // 1=易损, 2=非易损, 3=不展示
}
```

### 2.2 验证修改
- [ ] TypeScript 编译通过
- [ ] 相关组件无编译错误

## 阶段三：productId字段显示修复 (风险：低)

### 3.1 在列表显示中添加productId
**文件：`frontend/src/pages/SpareParts/index.tsx`**

**位置：** 在列表渲染函数中 (约第1650行附近)

```typescript
// 在名称显示后添加
<div className="flex items-start">
  <strong className="w-20 text-label font-medium flex-shrink-0">
    {String(t('fields.productId', { ns: 'spareParts' }) || 'Product ID')}:
  </strong>
  <span className="text-content font-medium line-clamp-1 ml-2">
    {part.id || 'N/A'}
  </span>
</div>
```

### 3.2 验证修改
- [ ] productId 在列表中正确显示
- [ ] 值来源于 `part.id` 字段
- [ ] 格式符合UI设计

## 阶段四：is_consumable筛选逻辑修正 (风险：中)

### 4.1 修正筛选状态定义
**文件：`frontend/src/pages/SpareParts/index.tsx`**

```typescript
// 修正前 (约第160行)
const [selectedIsConsumable, setSelectedIsConsumable] = useState<number | null>(null);

// 修正后
const [selectedIsConsumable, setSelectedIsConsumable] = useState<1 | 2 | null>(null);
```

### 4.2 修正筛选逻辑
**文件：`frontend/src/pages/SpareParts/index.tsx`**

**位置：** `getFilteredParts` 函数中 (约第880行)

```typescript
// 修正筛选逻辑
if (selectedIsConsumable !== null) {
  filteredResults = filteredResults.filter(part => {
    return part.is_consumable === selectedIsConsumable;
  });
}

// 同时过滤掉不展示的项目 (is_consumable === 3)
filteredResults = filteredResults.filter(part => {
  return part.is_consumable !== 3;
});
```

### 4.3 修正筛选UI
**文件：`frontend/src/pages/SpareParts/index.tsx`**

**位置：** 筛选器渲染部分 (约第2650行)

```typescript
<button
  onClick={() => setSelectedIsConsumable(1)}
  className={`px-4 py-2 rounded text-sm ${selectedIsConsumable === 1 ? 'bg-primary text-white' : 'bg-background text-content border border-gray-300'}`}
>
  {String(t('filters.consumable', { ns: 'spareParts' }) || '易损件')}
</button>
<button
  onClick={() => setSelectedIsConsumable(2)}
  className={`px-4 py-2 rounded text-sm ${selectedIsConsumable === 2 ? 'bg-primary text-white' : 'bg-background text-content border border-gray-300'}`}
>
  {String(t('filters.nonConsumable', { ns: 'spareParts' }) || '非易损件')}
</button>
```

### 4.4 验证修改
- [ ] 筛选器按钮正确显示
- [ ] 筛选逻辑正确工作
- [ ] 不展示 is_consumable=3 的项目

## 阶段五：字段显示完整性验证 (风险：低)

### 5.1 检查所有必需字段
运行完整性检查脚本：

```javascript
// 在浏览器控制台执行
const report = generateComplianceReport(window.sparePartsData || []);
printReport(report);
```

### 5.2 验证各个展示区域

#### 列表显示验证 (8个字段)
- [ ] 适配机型 (app_model)
- [ ] 产品图片 (image_url) 
- [ ] 料号 (part_number)
- [ ] 名称 (name_zh/name_en)
- [ ] Spec. (spec)
- [ ] Spec.(英制) (spec_imperial)
- [ ] **productId (id)** - 新添加
- [ ] 单箱数量 (pcs_per_box)

#### Tooltip显示验证 (5个字段)
- [ ] 适配序列号 (app_sn)
- [ ] 包装尺寸 cm (package_size_cm)
- [ ] 包装尺寸 inch (package_size_inch)
- [ ] 单件净重 kg (net_weight_kg)
- [ ] 单件净重 lbs (net_weight_lbs)

#### 购物车显示验证 (11个字段)
- [ ] 适配机型
- [ ] 产品图片
- [ ] 料号
- [ ] 名称
- [ ] Spec.
- [ ] 适配序列号
- [ ] 包装尺寸 cm
- [ ] 包装尺寸 inch
- [ ] 单件净重 kg
- [ ] 单件净重 lbs
- [ ] 单箱数量

## 阶段六：功能测试 (风险：低)

### 6.1 筛选功能测试
- [ ] 主机/配件类型筛选正常
- [ ] 适用机型筛选正常
- [ ] 易损类型筛选正常 (1=易损, 2=非易损)
- [ ] 筛选组合功能正常

### 6.2 显示功能测试
- [ ] 列表字段完整显示
- [ ] Tooltip 正确弹出并显示所有字段
- [ ] 购物车显示所有必需字段
- [ ] 公制/英制单位切换正常

### 6.3 数据一致性测试
- [ ] 字段值与后端数据一致
- [ ] 空值处理正确
- [ ] 数据类型正确

## 阶段七：性能和兼容性验证 (风险：低)

### 7.1 性能测试
- [ ] 页面加载速度无明显影响
- [ ] 筛选响应速度正常
- [ ] 大量数据显示正常

### 7.2 兼容性测试
- [ ] Chrome 浏览器正常
- [ ] Firefox 浏览器正常
- [ ] Safari 浏览器正常
- [ ] 移动端显示正常

## 回滚计划

### 如果出现问题，立即回滚
```bash
# 恢复备份文件
cp frontend/src/pages/SpareParts/index.tsx.backup.YYYYMMDD_HHMMSS frontend/src/pages/SpareParts/index.tsx
cp frontend/src/types/spareParts.ts.backup.YYYYMMDD_HHMMSS frontend/src/types/spareParts.ts

# 重新编译
npm run build
```

## 验收标准

### 必须通过的检查项
1. **字段完整性**：所有必需字段都正确显示
2. **筛选功能**：3个筛选项都正常工作
3. **数据类型**：is_consumable 使用数字类型 (1/2/3)
4. **productId显示**：列表中正确显示产品ID
5. **无编译错误**：TypeScript 编译无错误
6. **无运行时错误**：页面运行无控制台错误

### 可选的优化项
1. 字段显示顺序优化
2. UI布局微调
3. 错误处理增强

## 风险控制

### 低风险操作
- TypeScript 接口修正
- 显示字段添加
- 验证脚本运行

### 中风险操作  
- 筛选逻辑修改
- 状态管理变更

### 高风险操作
- 无（当前计划中没有高风险操作）

## 完成后验证

### 最终检查清单
- [ ] 运行字段完整性检查脚本，报告 0 个关键问题
- [ ] 所有手动测试用例通过
- [ ] 代码审查通过
- [ ] 性能测试通过

## 时间估算

- 阶段一：30分钟
- 阶段二：1小时
- 阶段三：1小时  
- 阶段四：2小时
- 阶段五：1小时
- 阶段六：2小时
- 阶段七：1小时

**总计：约8.5小时**

## 注意事项

1. **每个阶段完成后都要测试**，确保没有引入新问题
2. **保留详细的修改日志**，方便问题追踪
3. **任何阶段出现问题立即停止**，分析原因后再继续
4. **修改前先在开发环境验证**，确认无误后再部署
5. **建议分多次提交**，每个阶段单独提交，方便版本控制 