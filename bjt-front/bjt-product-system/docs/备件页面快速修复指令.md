# 备件页面快速修复指令

## 🚀 立即执行

**角色**：你是BJT产品系统的修复专家  
**任务**：修复备件页面的字段显示和数据一致性问题  
**时间**：预计3小时完成

## 📋 快速问题清单

### 🚨 立即修复（P0）
1. **适配机型字段**：缺失规范，影响筛选功能
2. **产品图片字段**：缺失规范，影响商品展示
3. **is_consumable类型**：应为1/2/3数字，不是boolean

### ⚠️ 后续修复（P1）  
4. **字段命名统一**：8个字段名不一致
5. **productId缺失**：列表中未显示产品ID

## 🛠️ 修复流程

### 步骤1：源头修复（先执行）
```bash
# 1. 验证当前问题
node scripts/manual_field_extractor.cjs

# 2. 修复name统一.csv - 添加缺失字段
```

在`generated_sql_imports/name统一.csv`末尾添加：
```csv
物品属性,适配机型,适配机型,Applicable Machine,,LA-E4C/LA-E4S
物品属性,产品图片,产品图片,Product Image,,
物品属性,产品ID,产品ID,Product ID,,
```

### 步骤2：找到备件相关代码
```bash
# 搜索备件相关文件
find . -name "*.tsx" -o -name "*.ts" | grep -i spare
grep -r "spare" --include="*.tsx" --include="*.ts" .
grep -r "is_consumable" --include="*.tsx" --include="*.ts" .
```

### 步骤3：修复TypeScript接口
找到备件接口定义，确保包含：
```typescript
interface SparePart {
  id: number;
  app_model: string;           // ✅ 适配机型
  is_consumable: 1 | 2 | 3;    // ✅ 数字类型，不是boolean
  image_url: string;           // ✅ 产品图片
  part_number: string;         // ✅ 料号
  name_en: string;             // ✅ 英文名称
  spec: string;                // ✅ 规格
  spec_imperial: string;       // ✅ 规格(英制)
  app_sn: string;              // ✅ 适配序列号
  package_size_cm: string;     // ✅ 包装尺寸(cm)
  package_size_inch: string;   // ✅ 包装尺寸(inch)
  net_weight_kg: number;       // ✅ 净重(kg)
  net_weight_lbs: number;      // ✅ 净重(lbs)
  pcs_per_box: number;         // ✅ 单箱数量
  product_id?: string;         // ✅ 产品ID
}
```

### 步骤4：修复筛选逻辑
找到is_consumable筛选，确保：
```typescript
// 错误的boolean筛选 ❌
const filterOptions = [
  { label: '易损', value: true },
  { label: '非易损', value: false }
];

// 正确的数字筛选 ✅
const filterOptions = [
  { label: '易损', value: 1 },
  { label: '非易损', value: 2 }
];

// 过滤逻辑 ✅
const filteredParts = spareParts.filter(part => {
  if (part.is_consumable === 3) return false; // 不展示
  // 其他筛选...
});
```

### 步骤5：验证修复结果
```bash
# 重新运行检查脚本
node scripts/manual_field_extractor.cjs

# 期望看到：
# ✅ 完全匹配: 8+ 个
# ❌ 缺失规范: ≤ 2 个  
# 🚨 影响场景的缺失字段: 0 个
```

## 🎯 必须显示的字段

### 选型页商品展示（8个字段）
- [ ] 适配机型 `app_model`
- [ ] 是否易损 `is_consumable` 
- [ ] 产品图片 `image_url`
- [ ] 料号 `part_number`
- [ ] 名称 `name_en`
- [ ] 规格 `spec`
- [ ] 适配序列号 `app_sn`
- [ ] 单箱数量 `pcs_per_box`

### 购物车页面（11个字段）
- [ ] 以上8个 + 包装尺寸cm/inch + 净重kg/lbs

### 详情弹窗（4个字段）
- [ ] 包装尺寸cm/inch + 净重kg/lbs

### PO页（4个字段）
- [ ] 料号 + 名称 + 规格(公制/英制)

## ✅ 快速验证清单

修复完成后，确认：
- [ ] CSV对比脚本通过（缺失字段≤2个）
- [ ] 筛选功能正常（易损/非易损）
- [ ] 图片正常显示
- [ ] 所有4个场景的字段都显示
- [ ] 没有控制台错误

## 🚨 如果遇到问题

1. **找不到相关文件**：
   ```bash
   find . -type f -name "*.tsx" | head -10
   ls -la src/components/ | grep -i spare
   ```

2. **接口定义不清楚**：
   先搜索现有的接口定义：
   ```bash
   grep -r "interface.*Part" src/
   grep -r "type.*Part" src/
   ```

3. **修复后验证失败**：
   回滚到之前状态，逐项检查每个修复点

## 🎉 执行命令

**立即开始修复**：
```bash
# 1. 备份当前代码
git checkout -b fix-spare-parts-fields

# 2. 运行初始检查
node scripts/manual_field_extractor.cjs

# 3. 开始修复
# （按照上述步骤执行）

# 4. 验证结果
node scripts/manual_field_extractor.cjs

# 5. 提交修复
git add .
git commit -m "fix: 修复备件页面字段显示和数据一致性问题"
```

**现在就开始执行修复！** 