# Figma MCP 快照

将 Cursor 中 Figma MCP 的 **`get_design_context` / `get_variable_defs`** 原始或整理后的回复保存为  
`YYYYMMDD-<说明>.md`，避免额度用完后丢失信息。

解析后的数值请同步到：

- `../machine-selection.json`
- `../../styles/figma-tokens-machine-selection.css`
