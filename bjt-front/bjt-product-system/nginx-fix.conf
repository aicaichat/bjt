# 针对WordPress REST API的Nginx配置修复
# 将此文件的内容添加到Nginx服务器的配置中

server {
    listen 80;
    server_name _;
    root /var/www/html;
    index index.php;

    # REST API特殊处理
    location /wp-json/ {
        # 改进的处理方式
        rewrite ^/wp-json/(.*) /index.php?rest_route=/$1 last;
        
        # 确保JSON内容类型
        add_header Content-Type 'application/json; charset=utf-8' always;
        
        # CORS支持
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With' always;
        
        # 如果是OPTIONS请求，直接返回200
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
            add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With' always;
            add_header 'Access-Control-Max-Age' 1728000 always;
            add_header 'Content-Type' 'text/plain charset=UTF-8' always;
            add_header 'Content-Length' 0 always;
            return 204;
        }
    }

    # PHP处理
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    # 静态文件处理
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
}

# BJT API特殊处理
location /wp-json/bjt/ {
    # 使用更严格的重写规则
    rewrite ^/wp-json/bjt/(.*) /index.php?rest_route=/bjt/$1 last;
    
    # 确保JSON内容类型
    add_header Content-Type 'application/json; charset=utf-8' always;
    
    # CORS支持
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
    add_header 'Access-Control-Allow-Credentials' 'true' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With' always;
}

# 测试API特殊处理
location /wp-json/bjt-test/ {
    # 使用更严格的重写规则
    rewrite ^/wp-json/bjt-test/(.*) /index.php?rest_route=/bjt-test/$1 last;
    
    # 确保JSON内容类型
    add_header Content-Type 'application/json; charset=utf-8' always;
    
    # CORS支持
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
    add_header 'Access-Control-Allow-Credentials' 'true' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With' always;
} 