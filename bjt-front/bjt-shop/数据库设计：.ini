数据库设计：

产品线表 
标题，说明，子项1，子项2，子项3，图片

主机表：
主机型号表
型号，名称，描述，图片1，图片2

主机料号表

关联关系表
配件表
配件型号表
配件料号表
耗材表
耗材料号表
备件表
备件料号表
必选备件表



API设计：


后端管理页面：


前端页面

气垫机必选备件关系表：
字段：product_line_id（产品线id），model_id（型号id），part_number_id（料号id），relation_type（关联类型），relation_value（关联值）
气垫机备件必选备件关系表：
字段：备件料号（产品线id），model_id（型号id），part_number_id（料号id），relation_type（关联类型），relation_value（关联值）

0气垫机产品线表
字段：标题，说明，子项1，子项2，子项3，图片

气垫机型号表
字段：型号（model），名称，描述，图片1，图片2

气垫机料号表：
字段：pic（图片），model（型号），brand（品牌），part_number（料号），name（名称），voltage（电压），package_size（包装尺寸），package_weight（包装毛重），pallet_size（托盘尺寸），pcs_per_pallet_1（一托数量1），pallet_height_1（打托高度1）

气垫机关联关系表：
字段：product_line_id（产品线id），model_id（型号id），part_number_id（料号id），relation_type（关联类型），relation_value（关联值）

气垫机配件料号表：
字段：pic（图片），model（型号），brand（品牌），part_number（料号），name（名称），voltage（电压），frequency（频率），package_size（包装尺寸），package_weight（包装毛重），pallet_size（托盘尺寸），pcs_per_pallet_1（一托数量1），pallet_height_1（打托高度1）

气垫机耗材料号表：
字段：pic（图片），model（型号），brand（品牌），part_number（料号），package_size（包装尺寸），package_weight（包装毛重），pallet_size（托盘尺寸），pcs_per_pallet_1（一托数量1），pallet_height_1（打托高度1），pcs_per_pallet_2，
pallet_height_2，pcs_per_pallet_3，pallet_height_3，app_model（适用机型），pak_shape，material
thickness_met，
thickness_imp，
gram_met，
gram_imp，
pcs_width_met，
pcs_width_imp，
pcs_length_met，
pcs_length_imp，
total_length_met，
total_length_imp，
inner_diameter，
roll_diameter，

气垫机备件料号表：
字段：consumable，pic（图片），part_number（料号），name（名称），package_size（包装尺寸），package_weight（包装毛重），app_model（适用机型），app_sn（适配序列号）

